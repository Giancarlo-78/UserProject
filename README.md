# UserProject

A RESTful microservice built with Java 21 and Spring Boot 3 for managing User records.

## Overview
This service exposes a complete CRUD API for the User entity, supports filtering by first name
and/or last name, and provides a CSV bulk import endpoint.
The application is fully containerized with Docker and uses MySQL as the persistence layer.
